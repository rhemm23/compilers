struct data {
  int a;
  bool b;
  bool data;
  struct c x;
}
void test () {
  int a;
  receive >> a(int);
}
int test1 (int a, bool b, int c) {
  receive >> "hello!";
  ret;
  ret fls;
}
void main () {
  bool d;
  struct data a;
  receive >> main(->void);
  receive >> data(struct);
  receive >> a(struct);
  receive >> test(->void)();
  receive >> 2;
  receive >> main(->void);
  receive >> data(struct);
  receive >> a(struct);
  a(struct)();
  test(->void)(1);
  test1(int,bool,int->int)((a(struct).a(int) + fls), 33, tru);
  ret a(struct).b(bool);
  ret (fls > tru);
  d(bool) = (8 && fls);
}
